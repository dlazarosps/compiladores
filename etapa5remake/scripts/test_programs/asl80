//Expressões lógicas podem ser formadas através dos operadores
//relacionais aplicados a expressões aritméticas, ou de operadores
//lógicos aplicados a expressões lógicas, recursivamente.
int f()
{
  if (a == 2) then { };
  if ((a+b) <= 10) then { };
}
