//Um bloco de comandos é considerado como um comando único simples,
//recursivamente, e pode ser utilizado em qualquer construção que
//aceite um comando simples.
int f ()
{
  {
    {
      {};
    };
  };
}
