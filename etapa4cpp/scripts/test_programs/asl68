//Nas duas listas dentro do for, não podem aparecer comandos simples
//que contenham vírgulas, como o output e o input, nem comando case.
//ERROR: output não permitido ali
int f()
{
  for (output x, y : x : x = x+1) { };
}
